// SPDX-License-Identifier: MIT
pragma solidity ^0.8.25;

contract AddressStorage {
    address public constant DEGEN = address(0);
    address public constant LDEGEN = address(0);
    address public constant DPAD = address(0);
    address public constant DPAD_STAKING = address(0);
    address public constant WETH = 0xdE41591ED1f8ED1484aC2CD8ca0876428de60EfF; // WGAS

    address public constant uniswapV3Factory = address(0);
    address public constant positionManager = address(0);
    address public constant swapRouter = address(0);
    address public constant quoterV2 = address(0);
}